<!DOCTYPE html>
<html>

<head>
    <title>NLP to SQL - Home</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-blue: #1e3a8a;
            --primary-blue-light: #3b82f6;
            --primary-blue-dark: #1e40af;
            --secondary-teal: #14b8a6;
            --neutral-50: #f9fafb;
            --neutral-800: #1f2937;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--primary-blue-dark) 100%);
            min-height: 100vh;
        }

        .navbar {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 16px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .logo {
            color: white;
            font-size: 24px;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .nav-links {
            display: flex;
            gap: 20px;
        }

        .nav-links a {
            color: white;
            text-decoration: none;
            padding: 8px 20px;
            border-radius: 8px;
            transition: all 0.3s;
            font-weight: 600;
        }

        .nav-links a:hover {
            background: rgba(255, 255, 255, 0.15);
        }

        .hero {
            text-align: center;
            padding: 100px 20px;
            color: white;
        }

        .hero h1 {
            font-size: 56px;
            margin-bottom: 20px;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
            font-weight: 800;
        }

        .hero p {
            font-size: 20px;
            margin-bottom: 40px;
            opacity: 0.95;
        }

        .cta-button {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: white;
            color: var(--primary-blue);
            padding: 16px 40px;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 700;
            font-size: 18px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            transition: all 0.3s;
        }

        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.4);
        }

        .features-grid {
            max-width: 1200px;
            margin: 50px auto;
            padding: 0 20px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .feature-card {
            background: rgba(255, 255, 255, 0.12);
            backdrop-filter: blur(10px);
            padding: 32px;
            border-radius: 16px;
            color: white;
            transition: all 0.3s;
            border: 1px solid rgba(255, 255, 255, 0.15);
        }

        .feature-card:hover {
            transform: translateY(-5px);
            background: rgba(255, 255, 255, 0.18);
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
        }

        .feature-card i {
            font-size: 40px;
            margin-bottom: 15px;
        }

        .feature-card h3 {
            font-size: 22px;
            margin-bottom: 10px;
            font-weight: 700;
        }

        .feature-card p {
            opacity: 0.9;
            line-height: 1.6;
        }
    </style>
</head>

<body>
    <div class="navbar">
        <div class="logo">
            <i class="fas fa-database"></i>
            NLP to SQL
        </div>
        <div class="nav-links">
            <% if(user) { %>
                <a href="/profile"><i class="fas fa-user"></i> Profile</a>
                <a href="/auth/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
                <% } else { %>
                    <a href="/auth/login"><i class="fas fa-sign-in-alt"></i> Login</a>
                    <% } %>
        </div>
    </div>

    <div class="hero">
        <h1>Query Databases Naturally</h1>
        <p>Transform natural language into SQL queries instantly</p>

        <% if(!user) { %>
            <a href="/auth/login" class="cta-button">
                <i class="fas fa-rocket"></i>
                Get Started
            </a>
            <% } else { %>
                <a href="/database/config" class="cta-button">
                    <i class="fas fa-database"></i>
                    Configure Database
                </a>
                <% } %>
    </div>

    <div class="features-grid">
        <div class="feature-card">
            <i class="fas fa-comments"></i>
            <h3>Natural Language</h3>
            <p>Ask questions in plain English and get SQL queries instantly</p>
        </div>
        <div class="feature-card">
            <i class="fas fa-shield-alt"></i>
            <h3>Secure & Safe</h3>
            <p>Built-in security features protect sensitive data automatically</p>
        </div>
        <div class="feature-card">
            <i class="fas fa-database"></i>
            <h3>Multiple Databases</h3>
            <p>Support for MySQL, PostgreSQL, MongoDB, and more</p>
        </div>
    </div>
</body>

</html>