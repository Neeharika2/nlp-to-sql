<!DOCTYPE html>
<html>

<head>
    <title>Profile</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        .nav {
            display: flex;
            justify-content: flex-end;
            gap: 15px;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #f0f0f0;
        }

        .nav a {
            text-decoration: none;
            color: #667eea;
            font-weight: 600;
            transition: all 0.3s;
        }

        .profile {
            text-align: center;
        }

        .profile img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            border: 5px solid #667eea;
            margin-bottom: 20px;
        }

        h1 {
            color: #2c3e50;
            margin-bottom: 10px;
        }

        h2 {
            color: #2c3e50;
            margin: 20px 0 10px 0;
        }

        .profile p {
            color: #7f8c8d;
            margin: 5px 0;
        }

        .role-badge {
            display: inline-block;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 8px 20px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
            margin: 15px 0;
        }

        .permissions {
            margin-top: 30px;
            padding: 25px;
            background: #f8f9fa;
            border-radius: 15px;
            text-align: left;
            border: 2px solid #e1e8ed;
        }

        .permissions h3 {
            color: #2c3e50;
            margin-bottom: 15px;
        }

        .permissions ul {
            list-style: none;
            padding: 0;
        }

        .permissions li {
            padding: 10px;
            margin: 5px 0;
            border-radius: 8px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .allowed {
            color: #27ae60;
            background: #d4edda;
        }

        .blocked {
            color: #e74c3c;
            background: #f8d7da;
        }

        .permissions li i {
            font-size: 18px;
        }

        .config-btn {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 15px 40px;
            border-radius: 50px;
            text-decoration: none;
            margin-top: 25px;
            font-weight: 600;
            transition: all 0.3s;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
        }

        .config-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);
        }

        .note {
            font-size: 12px;
            color: #7f8c8d;
            margin-top: 15px;
            padding: 15px;
            background: #fff3cd;
            border-radius: 8px;
            border-left: 4px solid #ffc107;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="nav">
            <a href="/"><i class="fas fa-home"></i> Home</a>
            <a href="/auth/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </div>

        <div class="profile">
            <% if(user) { %>
                <img src="<%= user.photo %>" alt="Profile photo">
                <h1>
                    <%= user.displayName %>
                </h1>
                <p><i class="fas fa-envelope"></i>
                    <%= user.email %>
                </p>
                <p><i class="fas fa-id-card"></i> ID: <%= user.id %>
                </p>
                <span class="role-badge"><i class="fas fa-crown"></i> ADMIN</span>

                <div class="permissions">
                    <h3><i class="fas fa-shield-alt"></i> Your Access Permissions</h3>
                    <ul>
                        <li class="allowed">
                            <i class="fas fa-check-circle"></i>
                            <span>Full access to all tables and views</span>
                        </li>
                        <li class="allowed">
                            <i class="fas fa-check-circle"></i>
                            <span>Can access regular data (names, emails, addresses, phones, etc.)</span>
                        </li>
                        <li class="blocked">
                            <i class="fas fa-times-circle"></i>
                            <span>Cannot access passwords or password hashes</span>
                        </li>
                        <li class="blocked">
                            <i class="fas fa-times-circle"></i>
                            <span>Cannot access credit card numbers or PCI data</span>
                        </li>
                        <li class="blocked">
                            <i class="fas fa-times-circle"></i>
                            <span>Cannot access SSN or social security numbers</span>
                        </li>
                        <li class="blocked">
                            <i class="fas fa-times-circle"></i>
                            <span>Cannot access API keys, tokens, or credentials</span>
                        </li>
                    </ul>
                    <div class="note">
                        <strong><i class="fas fa-info-circle"></i> Note:</strong> All sensitive data is blocked for
                        security.
                        When sensitive columns are requested, aggregate alternatives will be suggested.
                        All queries are logged for audit purposes.
                    </div>
                </div>

                <a href="/database/config" class="config-btn">
                    <i class="fas fa-database"></i>
                    Configure Database
                </a>
                <% } %>
        </div>
    </div>
</body>

</html>