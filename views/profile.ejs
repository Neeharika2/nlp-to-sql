<!DOCTYPE html>
<html>

<head>
    <title>Profile</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            text-align: center;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
        }

        .nav {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 20px;
        }

        .nav a {
            margin-left: 10px;
            text-decoration: none;
            color: #4285f4;
        }

        .profile {
            margin-top: 50px;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
            text-align: left;
        }

        .profile img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
        }

        .config-btn {
            display: inline-block;
            background-color: #4285f4;
            color: white;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            margin-top: 20px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="nav">
            <a href="/">Home</a>
            <a href="/auth/logout">Logout</a>
        </div>

        <div class="profile">
            <h1>Welcome to your profile</h1>
            <% if(user) { %>
                <img src="<%= user.photo %>" alt="Profile photo">
                <h2>
                    <%= user.displayName %>
                </h2>
                <p>Email: <%= user.email %>
                </p>
                <p>Google ID: <%= user.id %>
                </p>
                <a href="/database/config" class="config-btn">Configure Database</a>
                <% } %>
        </div>
    </div>
</body>

</html>